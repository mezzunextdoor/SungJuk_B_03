요구사항
성적처리 시스템 요구사항
교수는 성적입력한다.
- 교수 id, 학생id, 성적(자바, DB, 보안)을 입력한다.
- 반드시 교수id를 체크한다. (교수id는 "inha")
- 총점과 평균을 계산한다.
교수는 학점조회 한다.
- 반드시 교수id를 체크한다.
- 학점을 부여한다.(A학점:90점이상, B학점:80점 이상, C학점:80점 미만)

모든 명칭은 한글로 : Use case명, 클래스명, 속성명, 행위명, (데이터타입은 영문)
사용사례 다이어그램을 설계하시오. 유즈 케이스는 성적입력, 학점조회, 교수체크만 있음
클래스 다이어그램을 설계하시오. 클래스는 교수(행위:교수체크)", 성적(행위,성적입력, 학점조회)"와 <<boundary>> 클래스("Add_Sungjuk_UI)", "Search_Sungjuk_UI")만 있음.
순차도를 설계하시오. (성적입력, 학점조회)
순차도를 기반으로 구현하시오. (성적입력, 학점조회)
실행은 JSP로 실행할 것. (입력창은 생성하지 말고 처리 결과만 텍스트로 출력할 것.


모든 명칭은 한글로 : Use case명, 클래스명, 속성명, 행위명, (데이터타입은 영문)
클래스 다이어그램을 설계하시오. 클래스는 교수(행위:교수체크)", 성적(행위,성적입력, 학점조회)"와 <<boundary>> 클래스("Add_Sungjuk_UI)", "Search_Sungjuk_UI")만 있음.

class diagram Mermaid 문법에 맞게 작성해줘
- class 속성에 대해 아래와 같이 작성해줘
"가시성 속성변수: 데이터타입"
-class 행위에 대해 아래와 같이 작성해줘
"가시성 함수명(매개변수) 반환데이터타입"

위 요구사항에 대해 Class diagram Mermaid 코드를 작성해줘
같이 보낸 use case 다이어그램 참고해서 작성해줘.



위의 요구사항을 기반으로 사용사례 다이어그램 머메이드 코드를 생성해줘. 유즈 케이스는 성적입력, 학점조회, 교수체크 세 개만 있고, 성적입력과 학점조회가 교수체크를 include 하는 관계로 작성해줘.

위의 요구사항과 사용사례 다이어그램을 기반으로 클래스 다이어그램 머메이드 코드를 생성해줘.
클래스는 교수(행위:교수체크)", 성적(행위,성적입력, 학점조회)"와 <<boundary>> 클래스("Add_Sungjuk_UI)", "Search_Sungjuk_UI")만 있음.

class diagram Mermaid 문법에 맞게 작성해줘
- class 속성에 대해 아래와 같이 작성해줘
"가시성 속성변수: 데이터타입"
-class 행위에 대해 아래와 같이 작성해줘
"가시성 함수명(매개변수) 반환데이터타입"


위의 요구사항과 클래스 다이어그램을 기반으로 '성적입력'에 대한 순차도 머메이드 코드를 작성해줘. Add_Sungjuk_UI 바운더리 클래스로부터 시작되게 해줘.
위의 Java 코드를 사용해서 boundary 클래스 두 개를 JSP로 구현해줘. 입력창은 만들지 말고 하드코딩된 테스트 데이터로 처리 결과만 텍스트로 출력할 것. 스크립트릿으로 성적 클래스의 메서드를 직접 호출해줘.